# FABS Valid Settings
There are a lot of possible settings in `settings.yaml`. Below, you will find a comprehensive reference guide for the available parameters that you can configure within `settings.yaml`.

This is open-source, so if something isn't available for tinkering in the YAML config file and you're comfortable with Python (the only language I used for this utility)...knock yourself out! :)